import * as Kdt from './kdt';

const { balanceKdtree, insertNode, deleteNode } = Kdt;

type Coordinate = Kdt.Coordinate
type TreeNode = Kdt.TreeNode
type Photon = Kdt.Photon

const photonCatchRatio = 0.4; 
